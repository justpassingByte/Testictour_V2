import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const m = await prisma.match.findFirst({ orderBy: { fetchedAt: 'desc' }, include: { lobby: { include: { round: { include: { phase: true } } } } } }); if (!m) return; console.log('Tournament:', m.lobby.round.phase.tournamentId); const r = await fetch('http://localhost:4000/api/dev/tournament-statistics/' + m.lobby.round.phase.tournamentId); console.log(await r.json()); } main().finally(() => prisma.$disconnect());
